function calculatecartprice(...num1)
{
    return num1;
}

console.log(calculatecartprice(100,200,300,400,500,600,700,800,900));


const user={
    username: "sufiyan",
    price:199
}

function handleobject(user)
{
    console.log('username is ${user.uesrname} and price is ${user.price}');

}
handleobject(user);

const mynewarr=[200,400,100,600]
function handlearray(mynewarr)
{
    return getArray[1]
}

console.log(handlearray(mynewarr));


